2017/7/14
1.添加CAN接收溢出错误处理
2.添加CAN方向和串口方向超时计数，超时后复位

2017/6/14
1.添加周期状态返回
2.主动召唤返回F1 00 11

2017/6/13
1.恢复正常输出。采用UART1 发送与接收
2.使用宏定义区分不同项目。

2017/6/6
1.针对CAN-485添加相应的超时控制。
2.开启看门狗WDT 周期 = 2 ms ·预分频比 A·预分频比 B 2*1*8 = 16ms

2017/5/14
1.修正字节长度限制8，改成 8 + 2

2017/5/10 建立文件目录
1.
 * 数据结构：目的地址（1byte 0xA1）--功能码(1byte 0x55)--数据长度(1byte) + (数据包) + 16bit累加和（ 2bytes）
 * 数据包： ID（low byte） + ID （Hight Byte） + 数据(1-8) 
2.
CAN接收数据包转发。

3.实现CAN报文转发功能。
